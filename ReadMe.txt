


The DLL creates internal styles and JS references, important so the referenced assembly matches the styles/js it needs.

The adminFramework collection ONLY includes dependancies for Google api, and must be handled this way to prevent duplcate references.